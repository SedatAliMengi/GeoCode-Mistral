# GeoCode-Mistral — Python dependencies
# Versions pinned to the environment the project was developed and tested on.
#
# IMPORTANT: torch must be installed as a CUDA build, not from plain PyPI.
# Install it first from the official index, e.g.:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# then:  pip install -r requirements.txt

# --- Core training / model stack ---
torch==2.5.1            # tested build: 2.5.1+cu121 (CUDA 12.1)
transformers==4.57.1
peft==0.17.1
trl==0.25.1
bitsandbytes==0.48.1
accelerate==1.11.0
datasets==4.3.0
safetensors==0.6.2
huggingface-hub==0.36.0

# --- Interface ---
gradio==6.3.0

# --- Utilities ---
protobuf==4.25.9
scipy==1.13.1
